# Pi Web V2 产品升级方案

版本：v0.1（待确认稿）  
日期：2026-08-02  
依据：《Pi Web V2 功能架构文档》  
范围：产品架构、页面优先级与 Web 交互规划；不包含代码、视觉稿或模拟业务数据。

## 1. 升级目标

Pi Web V2 不再按照桌面端的“页面功能堆叠”方式迁移，而改为“运营任务驱动”的金融工作台：

1. Dashboard 负责发现状态、异常和待处理事项，并提供业务入口。
2. Centro de Clientes 负责人物、分组、资金和交易关系的统一查看。
3. Centro de Operaciones 负责 Compra、持仓和 Venta 的连续操作闭环。
4. Mercado EE.UU. 与 Mercado México 负责真实市场状态和行情上下文。
5. Reserva Institucional 保持为独立业务域，不与普通交易数据混用。
6. Centro de Plantillas 负责现有交易、机构和 VIP 模板的管理与制作。
7. Reportes 负责统一解释 Dashboard 中的汇总数据并提供明细导出。

升级只减少页面跳转、重复输入和上下文丢失，不改变以下业务逻辑：

- 人物资格、开户状态、VIP、余额、最低股数和最大资金比例。
- 当前已发布剧本参与者作为 Compra 推荐来源。
- 买入时同步写入人物资金、订单、交易和持仓。
- Venta 按持仓记录选择人员并同步更新资金、交易、卖出名单和持仓。
- 美股、墨股、USD/MXN 的货币与汇率关系。
- Reserva Institucional 的独立存储边界。
- 无真实数据时显示空状态或断开状态，不使用伪造数据。

## 2. Web V2 新版功能架构

### 2.1 一级导航

| 导航 | 主要职责 | 核心入口 |
|---|---|---|
| Dashboard | 运营总览和待处理入口 | 人物、Compra、Venta、持仓、市场、机构预留 |
| Centro de Clientes | 人物、分组、资金、交易关系 | 人物目录、人物详情、关联交易 |
| Centro de Operaciones | 交易和资产闭环 | Resumen、Compra、Posiciones、Venta |
| Mercado EE.UU. | 美股指数和市场状态 | S&P 500、周期、行情详情、Snapshot |
| Mercado México | 墨股指数和市场状态 | IPC México、周期、行情详情、Snapshot |
| Reserva Institucional | 机构预留独立工作台 | 配置、容量、机构状态、窗口 |
| Centro de Plantillas | 模板目录和编辑 | 买入、卖出、机构、VIP 模板 |
| Reportes | 运营和资产分析 | KPI、趋势、交易明细、导出 |
| Configuración | 用户偏好和数据管理 | 导入、备份、恢复、分页/刷新设置 |

### 2.2 二级业务结构

```text
Dashboard
├── 核心资产
├── 今日运营
├── 待处理事项
├── 业务入口
└── 市场状态

Centro de Clientes
├── 人物目录
├── 分组结构
├── 人物详情
│   ├── 基本信息
│   ├── 资金与账户
│   ├── 持仓
│   ├── 交易记录
│   └── 资料日志
└── 关联交易入口

Centro de Operaciones
├── Resumen
├── Compra
│   ├── 交易设置
│   ├── 人员推荐与调整
│   └── 最终确认
├── Posiciones
│   ├── 当前持仓
│   ├── 72 小时待处理
│   └── 股票/人物下钻
└── Venta
    ├── 选择标的
    ├── 选择持仓人员
    ├── 参数与预览
    └── 执行结果/卖出名单

Mercados
├── Mercado EE.UU.
└── Mercado México

Centro de Plantillas
├── 模板目录
├── 快速预览
└── 模板编辑器

Reportes
├── 运营统计
├── 资产统计
├── 人物参与统计
└── 交易明细与导出
```

### 2.3 路由与页面状态

| 能力 | 建议地址 | 说明 |
|---|---|---|
| Dashboard | `/dashboard` | 保持现有路由 |
| 人物目录 | `/clients` | 筛选和分页写入查询参数 |
| 人物详情 | `/clients/[id]` | Tab 写入查询参数 |
| 交易中心 | `/trading` | `view=overview|buy|positions|sell` 保存业务子视图 |
| 美股 | `/market/us` | 周期写入查询参数 |
| 墨股 | `/market/mexico` | 周期写入查询参数 |
| 机构预留 | `/institutional` | 独立服务和品牌区 |
| 模板中心 | `/templates` | 目录与编辑状态在同一业务域 |
| 报表中心 | `/reports` | 补回桌面端已有 Reportes 能力 |
| 设置 | `/settings` | 保持现有路由 |

新增 `/reports` 是补回桌面系统已有模块，不属于新增业务。

## 3. 页面优先级

### 3.1 优先级排序

| 顺序 | 模块 | 优先级 | 原因 |
|---|---|---|---|
| 1 | Dashboard | P0 | 所有模块的运营入口，必须先定义统一指标和待处理结构 |
| 2 | Centro de Clientes + 人物详情 | P0 | Compra、Venta、持仓和报表都依赖人物与资金规则 |
| 3 | Centro de Operaciones / Compra | P0 | 建立剧本推荐、人员卡和买入写入闭环 |
| 4 | Posiciones + Venta | P0 | 完成买入后的资产管理和卖出闭环 |
| 5 | Mercado EE.UU. | P1 | 为美股市场状态和 Compra 提供真实行情上下文 |
| 6 | Mercado México | P1 | 复用市场能力并验证 MXN 口径 |
| 7 | Reportes | P1 | 依赖前述真实数据，统一 Dashboard 与明细口径 |
| 8 | Centro de Plantillas | P2 | 依赖真实订单、卖出记录和人物数据进行预览 |
| 9 | Reserva Institucional | P2 | 当前真实数据源缺失，先保留独立结构和空状态 |

### 3.2 开发阶段

- 阶段 A：Dashboard、人物目录、人物详情。
- 阶段 B：Compra、持仓、Venta。
- 阶段 C：美股、墨股、Reportes。
- 阶段 D：模板中心、Reserva Institucional。

每个阶段开始前先冻结该阶段使用的数据字段和计算口径；页面完成后再进入下一阶段。

## 4. 桌面端复杂操作与 Web 减步策略

| 任务 | 桌面端主要问题 | Web V2 简化方式 | 业务逻辑变化 |
|---|---|---|---|
| 查看运营状态 | 资产、运营、市场和待卖信息分散 | Dashboard 一屏汇总，点击指标进入已筛选页面 | 无 |
| 查人物交易 | 目录 → 详情 → Tab → 交易/持仓 | 人物详情保留 Tab；列表可直接打开交易关系摘要 | 无 |
| 创建 Compra | 创建页 → 推荐页 → 确认页 → 完成页，重复查看摘要 | 配置和推荐同屏，固定摘要栏；最终确认保留一次 | 无 |
| 调整推荐人员 | 卡片、筛选和金额摘要分散，切换后容易失去上下文 | 人员卡片即时选择，顶部/右侧摘要同步更新 | 无 |
| 执行 Venta | 持仓 → 股票详情 → 卖出页 → 人员选择 → 参数 → 日志 | 持仓直接进入预填 Venta；人员、参数和摘要同屏 | 无 |
| 查看 72 小时持仓 | Dashboard 和隐藏持仓页之间关系不清晰 | Dashboard 待处理列表直接打开 Posiciones 的待处理 Tab | 无 |
| 切换市场 | 同一页面 Tab 切换但无法形成独立地址 | 独立美股/墨股路由，共用组件与数据服务 | 无 |
| 查看报表依据 | Dashboard 与 Reportes 各自计算 | 共享指标定义，Dashboard 指标可下钻到 Reportes | 无 |
| 使用模板 | 目录与全屏编辑器切换，缺少保存状态可信度 | 目录先快速预览，再进入编辑；显示真实保存状态 | 无 |

## 5. Dashboard 升级方案

### 5.1 角色定位

Dashboard 不作为“装饰性数据墙”，而作为运营人员每天进入系统后的第一工作台：回答现在有多少资产、今天发生了什么、有哪些事情需要处理、下一步去哪里。

### 5.2 原桌面功能

- 今日新增 VIP、今日开户、今日参与交易、当前持仓股票、今日交易额。
- 总资产、可用资金、持仓市值、浮动盈亏。
- Compra、Venta 和交易中心入口。
- 买入后 72 小时倒计时。
- 低发言/低交易人物推荐。
- 交易参与、VIP 和开户趋势。
- 股票行情和 USD/MXN 市场条。

### 5.3 Web V2 保留

- 四项核心资产：总资产、可用资金、持仓市值、今日盈亏/浮动盈亏，按最终数据口径确定名称。
- 五项原运营指标：今日新增 VIP、今日开户、今日参与交易、持仓股票数、今日交易额。
- 72 小时待处理持仓。
- Compra、Venta、Centro de Clientes、两大市场和 Reserva Institucional 入口。
- 交易活跃、VIP、开户趋势。
- 美股、墨股和 USD/MXN 实时状态。

### 5.4 Web V2 优化

- 区分“资产状态”和“运营状态”，避免把不同口径混在同一组卡片。
- 资产卡只展示真实聚合值；数据源未连接时显示“未连接”，无记录时显示 0 和可执行入口。
- 待处理区按剩余时间排序，优先显示最紧急的真实持仓。
- 趋势图与 Reportes 使用同一指标服务，不在 Dashboard 单独计算。
- 市场状态只显示指数、涨跌、状态和更新时间，不在首页复制完整 K 线。
- 取消大段欢迎语、系统副标题和装饰性标题，内容从第一行开始。

### 5.5 新增 Web 交互

- 点击运营 KPI，进入 `/reports` 并自动带上对应指标和日期范围。
- 点击待处理股票，进入 `/trading?view=positions&tab=pending&ticker=...`。
- 点击 Compra/Venta，进入带来源上下文的交易子视图。
- 空状态不是空白卡片：提供“导入人物”“进入 Compra”“查看持仓”等现有业务入口。
- 页面保留最后一次选定的趋势周期，但不保存具体业务数据副本。

### 5.6 页面结构建议

```text
第一层：核心资产（4 张紧凑卡）
第二层：今日运营 KPI（5 项横向指标）
第三层：
├── 左：72 小时待处理持仓
└── 右：业务入口矩阵
第四层：
├── 交易参与趋势
├── VIP/开户趋势
└── 市场状态
```

### 5.7 删除或隐藏

- 删除模拟趋势、种子金额和任何硬编码增长率。
- 无真实来源时隐藏人物推荐金额估算，不使用默认 AAPL 或默认汇率冒充当前数据。
- “资金分配”没有独立桌面业务模型，不作为正式 Dashboard 模块；等待业务确认。

## 6. Centro de Clientes 升级方案

### 6.1 角色定位

人物中心从“客户表格”升级为人物运营档案：同一人物的身份、分组、账户资金、购买规则、持仓和交易关系必须可以连续查看。

### 6.2 原桌面功能

- 人物统计、小组分布和任务状态结构。
- 人物类型、VIP、开户/跟进筛选。
- 搜索、排序、分页、JSON/CSV 导入。
- 人物详情的基本信息、资产、持仓、交易、账户和日志。
- 编辑人物分组、账户、资金和购买规则。

### 6.3 Web V2 保留

- 四类人物、五个实际小组、VIP/普通、开户/未开户/跟进中。
- 原列表字段，特别保留“今日发言”，不以“今日盈亏”替换。
- 搜索、等级、小组、状态、人物类型、排序和分页。
- 人物详情五类内容：概览、交易、持仓/盈亏、账户、日志。
- JSON/CSV 导入和人物 ID 唯一性校验。
- 人物资金、最低股数、资金比例和票型对 Compra 的影响。

### 6.4 Web V2 优化

- 目录以高密度表格为主，小组和状态统计压缩为顶部小卡，不占用独立侧栏。
- 编号、头像/姓名固定在左侧；资金、交易和操作固定在右侧或保持可见。
- 列表只展示运营决策需要的字段，完整人物画像放入详情页。
- 分组筛选直接使用五个真实小组，移除未接线的 A/B/C 外观筛选。
- `maxFundRatio` 统一为小数存储、百分比输入与展示。
- 人物 ID 修改由统一服务处理所有关联引用，不能产生孤立订单或卖出日志。

### 6.5 新增 Web 交互

- 筛选、排序、分页写入 URL；从详情返回时保留原列表位置。
- 点击人物行进入详情；点击资金或交易区域可直接打开对应详情 Tab。
- 人物详情提供“进入 Compra”入口，只携带人物上下文，不跳过原推荐和资格校验。
- 持仓行可进入关联股票/持仓上下文；交易行可查看原订单摘要。
- 编辑使用右侧抽屉或紧凑对话框，保存后原地刷新人物摘要。
- 导入分两步：解析与校验 → 确认导入，避免错误文件直接覆盖人物池。

### 6.6 页面结构建议

```text
/clients
├── 人物统计条
├── 搜索与筛选工具栏
├── 高密度人物表格
└── 分页

/clients/[id]
├── 人物身份与账户摘要
├── 资金指标
├── Tab：概览 / 持仓 / 交易 / 账户 / 日志
└── 关联操作：编辑资料 / 进入 Compra
```

### 6.7 删除或隐藏

- 删除页面顶部大标题和英文装饰标题。
- 隐藏没有真实字段支撑的客户 CRM 标签、销售漏斗或跟单功能。
- 不把手机号、家庭、资产画像等长文本全部塞进目录表。

## 7. Centro de Operaciones 升级方案

### 7.1 角色定位

Centro de Operaciones 是 Compra、Posiciones 和 Venta 的统一业务域。用户不需要在多个独立页面间重新选择股票、人物或条件。

### 7.2 原桌面功能

- 交易资产总览、资产趋势、持仓概览和分布。
- Compra 创建任务、行情/汇率、剧本推荐、人员选择、订单确认和执行写入。
- 持仓列表、股票详情和 72 小时待处理。
- Venta 选择股票、持仓人员、折扣、交易预览、执行和卖出名单。

### 7.3 Web V2 保留

- Resumen、Compra、Posiciones、Venta 四个子视图。
- 原有市场、票型、股票、价格、汇率、折扣、参与人数和资金占比。
- 当前剧本人物池、人物资格和购买规则。
- 人员接受/拒绝、股数调整和最终确认。
- 买入/卖出的原有数据写入关系。
- 持仓和交易明细导出。

### 7.4 Web V2 优化

- 使用 `/trading` 内部 Tab/URL 状态，而不是分散的临时页面。
- 股票、市场、人物和筛选条件在 Compra → Posiciones → Venta 之间传递。
- 交易摘要固定在右侧，任何人员或参数变化都即时更新真实计算结果。
- 交易总览只负责资产状态和入口，不重复完整 Dashboard。
- 错误显示在对应字段或人员卡，不使用全局弹窗中断全部操作。
- 离开未完成 Compra/Venta 时保存草稿界面状态；不保存未经确认的交易记录。

### 7.5 新增 Web 交互

- 顶部子导航：Resumen / Compra / Posiciones / Venta。
- 从 Dashboard、人物详情或持仓进入时显示来源上下文和已预填项。
- 页面刷新后从 URL/Session 恢复交易阶段，但重新校验实时行情和资格。
- 最终提交按钮防重复点击；返回结果显示实际成功人数和被跳过原因。
- 交易执行后可直接查看新持仓或卖出名单，无需回 Dashboard 再进入。

### 7.6 页面结构建议

```text
固定区：Centro de Operaciones 子导航
内容区：当前子视图
右侧：统一交易摘要/执行栏
底部：必要的批量操作与选择汇总
```

### 7.7 删除或隐藏

- 隐藏没有实际事件链的限价单/市价单/条件单切换。
- 隐藏没有真实数据源的盘口和最近成交。
- “任务记录”在真实记录模型接线前不作为可操作 Tab。

## 8. Compra 升级方案

### 8.1 原桌面功能

- 四阶段：创建任务、推荐人员、确认订单、完成。
- Yahoo 行情和 USD/MXN 更新。
- 当前剧本推荐、资金与最低股数校验。
- 人员卡片、接受/拒绝、预计金额/股数。
- 执行后写入订单、交易、持仓和人物资金。

### 8.2 Web V2 保留

- 全部交易字段和原资格规则。
- 当前剧本是推荐来源。
- 人工调整名单和股数。
- 最终风险确认和一次执行。
- 无行情时禁止以伪造值继续。

### 8.3 Web V2 优化

- 将“设置”和“人员推荐”合并为一个工作台，不再切换两个完整页面。
- 交易设置压缩为顶部横向参数区：市场、票型、股票、折扣、人数、资金比例。
- 推荐结果紧接参数区，人员选择不离开当前页面。
- 右侧固定摘要持续展示价格、参与人数、资金比例、总金额、总股数和可用资金。
- 最终确认保留为一次明确确认层，不重复整页展示相同信息。

### 8.4 人员卡片调用规则

统一 `PersonTradeCard` 概念在 Compra 和 Venta 复用，但模式不同：

| 内容 | Compra | Venta |
|---|---|---|
| 身份 | 编号、头像、姓名、类型、小组、VIP | 同左 |
| 资金 | 可用资金、最大可用金额 | 持仓数量、成本和市值 |
| 规则 | 最低股数、最大资金比例、资格状态 | 可卖/锁定、待处理时间 |
| 输入 | 推荐股数/接受/拒绝 | 选择该持仓记录 |
| 状态 | 已选择、被排除、规则不足 | 已选择、锁定、已失效 |

人员卡片只显示真实人物和真实计算结果；没有头像时使用已有统一头像占位规则，不生成虚假人物。

### 8.5 新增 Web 交互

- 一键推荐后在当前区域填充人员卡；不跳转页面。
- 全选、清空、接受、拒绝和股数修改即时更新右侧摘要。
- 可按人物类型、小组、VIP 和搜索过滤，但过滤不改变已经选中的数据。
- 点击排除状态展示原规则原因，例如未开户、余额不足、低于最低股数。
- 确认前仅展示发生变化的关键字段和最终人员数。

### 8.6 页面结构建议

```text
顶部：Compra 设置（单行/双行紧凑参数）
中部：推荐说明 + 筛选 + 人员卡片网格
底部固定：全选 / 清空 / 已选人数 / 总金额 / 总股数
右侧固定：交易摘要 + 最终确认
```

### 8.7 减少步骤

- 桌面：创建 → 推荐 → 确认 → 完成。
- Web：配置与推荐同屏 → 最终确认 → 原地结果。
- 被合并的是页面步骤，不是资格检查和执行规则。

### 8.8 删除或隐藏

- 不显示未接线的任务历史、订单类型和盘口。
- 买入日期若仍不参与执行则隐藏；如需保留，必须先确认其业务含义。
- 严格资格池为空时不自动展示全体人物；是否保留桌面兜底需业务确认。

## 9. Venta 升级方案

### 9.1 原桌面功能

- 选择股票、选择持仓人员、设置折扣、预览金额、勾选协议、执行。
- 显示持仓总量、可卖数量、成本、当前价和浮盈。
- 执行后写入交易和卖出日志、回写余额、删除持仓。
- 卖出名单筛选、搜索和 CSV 导出。

### 9.2 Web V2 保留

- 只能从真实持仓中选择人物记录。
- 锁定持仓不可选择。
- 0–20% 折扣范围。
- 成交价、人数、股数、金额、手续费/净到账展示范围，等待口径确认。
- 确认勾选、不可逆提示、执行结果和卖出名单。

### 9.3 Web V2 优化

- 从持仓进入时自动预选股票，不再重复选择标的。
- 标的、人员和参数放在同一工作台，右侧持续显示 Venta 摘要。
- 人员卡复用 Compra 的身份结构，但展示持仓与可卖状态。
- 锁定或失效持仓直接标记原因，不允许页面绕过。
- 执行后切换到该次卖出结果或卖出名单，不重新进入列表查找。

### 9.4 新增 Web 交互

- 在 Posiciones 勾选单个股票的持仓后直接“创建 Venta”。
- 72 小时待处理列表进入时自动按剩余时间排序。
- 选择变化即时更新总股数、成交金额和预计到账。
- 提交前重新校验持仓状态；变化的记录从名单中标记并移除。
- 卖出成功结果提供“查看人物”“查看卖出名单”“返回持仓”三个现有关系入口。

### 9.5 页面结构建议

```text
顶部：Venta 标的摘要
左侧：持仓股票列表（直接进入时可收起）
中部：可卖人员卡片
右侧：折扣参数 + 交易预览 + 风险确认 + 执行
下方/独立 Tab：卖出人员名单
```

### 9.6 删除或隐藏

- 手续费规则未确认前，不把 0.2% 当作真实扣费写入。
- 18:00–20:00、T+2 和协议版本没有真实记录字段时，只能作为说明，不能显示为已执行状态。
- 不保留模拟成交或固定净到账金额。

## 10. 持仓管理升级方案

### 10.1 原桌面功能

- 当前持仓、72 小时待卖、统计卡、趋势、状态/人物分布。
- 股票、人物类型、状态、搜索、分页。
- 股票详情、参与持仓人物、操作记录。
- 批量卖出和持仓 CSV 导出。
- 原页面在主导航中隐藏。

### 10.2 Web V2 保留

- 当前持仓和待处理两个视角。
- 人物、股票、数量、成本、当前价、盈亏、收益率、状态。
- 股票详情、人物详情、Venta 和导出关系。
- 72 小时剩余时间与优先级。

### 10.3 Web V2 优化

- 正式放入 Centro de Operaciones 的 Posiciones 子视图，不新增一级导航。
- 默认显示按股票聚合的持仓；展开后显示人物持仓明细。
- “当前持仓”和“72 小时待处理”使用同一数据表与不同过滤状态。
- 移除模拟历史曲线；只有真实时间序列存在时才显示趋势。
- 所有 USD 持仓按统一汇率同时显示原币和 MXN 汇总。

### 10.4 新增 Web 交互

- 点击股票聚合行展开人物持仓，不必先进入独立股票详情页。
- 需要更多行情时再打开股票详情抽屉/子视图。
- 从聚合行直接创建 Venta；自动传入 ticker 和可卖持仓。
- 点击人物直接进入 `/clients/[id]` 的持仓 Tab。
- Dashboard 下钻自动进入待处理 Tab 并保持原筛选。

### 10.5 页面结构建议

```text
顶部：持仓人数 / 成本 / 市值 / 浮盈 / 可卖人数
Tab：当前持仓 / 72 小时待处理
工具栏：股票 / 人物类型 / 状态 / 搜索 / 导出
主体：股票聚合表 → 展开人物持仓
右侧或抽屉：股票摘要 / 操作记录 / Venta 入口
```

### 10.6 删除或隐藏

- 删除正弦函数生成的持仓趋势和备用模拟 K 线。
- 隐藏固定成交量、市值、日高和日低。
- 不使用“锁定中”之外的新持仓状态。

## 11. Mercado EE.UU. 升级方案

### 11.1 原桌面功能

- S&P 500 `^GSPC` 实时价格、涨跌、市场状态和更新时间。
- 日高/低、52 周高/低、USD。
- 1D、5D、1M、3M、6M、YTD、1Y、5Y、All。
- K 线、MA5、成交量、缩放、Retry、Snapshot。

### 11.2 Web V2 保留

- 全部指数 Meta、周期、图表、错误状态、重试和截图。
- Yahoo Finance 作为当前统一口径。
- 数据失败时清空实时状态，不使用本地模拟数据。

### 11.3 Web V2 优化

- 独立路由 `/market/us`，页面刷新后保留周期。
- 首屏只显示当前指数、状态、更新时间和主要 Meta；图表占据主要工作区。
- 市场开闭状态和时间按 New York 时区解释，同时保留 CDMX 当前时间作为系统上下文。
- 明确“指数行情”和“个股交易价格”是不同数据，不用指数页数据估值持仓。
- 移动完整市场说明到紧凑信息栏，减少图表周围空白。

### 11.4 新增 Web 交互

- 周期进入 URL 查询参数。
- 断开时显示错误原因、最后成功更新时间和 Retry；不显示旧值为实时值。
- Snapshot 只截取指数摘要和图表，不包含 Sidebar/Header。
- 提供“进入 Centro de Operaciones”导航，但不预设不存在的股票或订单。

### 11.5 页面结构建议

```text
左/上：S&P 500 摘要、涨跌、市场状态
中部主区域：K 线 + MA5 + 成交量
图表上方：周期切换
侧边信息：日高/低、52 周高/低、货币、更新时间
底部：数据源说明、Retry/Snapshot
```

### 11.6 删除或隐藏

- 删除模拟 K 线、随机价格和固定涨跌数据。
- 不展示未接入的个股榜单、自选股或新闻模块。

## 12. Mercado México 升级方案

### 12.1 原桌面功能

- IPC México `^MXX` 实时价格、涨跌、市场状态和更新时间。
- 日高/低、52 周高/低、MXN。
- 与美股相同的周期、K 线、MA5、成交量、Retry 和 Snapshot。

### 12.2 Web V2 保留

- 全部 IPC México 指数能力。
- Yahoo Finance 当前数据口径和失败状态。
- BMV、MXN 与 CDMX 时区语义。

### 12.3 Web V2 优化

- 独立路由 `/market/mexico`，底层复用 Market Service 和图表组件。
- 页面明确显示 BMV 和 Mexico City 时间，避免沿用美股时区文案。
- 指数 Meta 和 Compra 使用的墨股个股报价分开。
- 与美股保持相同信息结构，减少学习成本；仅保留市场、货币和时区差异。

### 12.4 新增 Web 交互

- 在美股/墨股导航之间切换时保留各自最后周期。
- Retry、Snapshot 和数据更新时间行为与美股一致。
- 进入 Centro de Operaciones 时只传递市场 `MX`，不自动选择股票。

### 12.5 页面结构建议

与 Mercado EE.UU. 使用相同布局：指数摘要、主图表、周期、Meta、数据状态；品牌区可使用已确认的市场专用 Logo，但不与主 Pi Logo 混用。

### 12.6 删除或隐藏

- 删除硬编码 BMV 个股数据、模拟趋势和伪实时状态。
- 不把 Reserva Institucional 的机构配额放入普通墨股市场页。

## 13. Reserva Institucional 升级方案

### 13.1 角色定位

Reserva Institucional 是独立的机构预留工作台。它可以共享 AppShell，但使用专用品牌、专用数据服务和专用权限边界；不读写普通 Compra/Venta 的人物或持仓。

### 13.2 原桌面功能

- CDMX 时间、开放/下次开放状态。
- 总金额和折扣编辑。
- 已预留股数、可用容量、总金额、BMV。
- 两张趋势图、机构状态表、搜索、状态筛选和排序。
- 夜间预留窗口、BMV 操作窗口和运营日历。

### 13.3 Web V2 保留

- 金额、折扣、预留、可用容量、机构状态、窗口和日历结构。
- 机构表全部原字段。
- 搜索、状态筛选、排序和显式保存。
- CDMX 时区和独立存储。

### 13.4 Web V2 优化

- 第一屏优先显示配置、当前窗口和容量，而不是两个大面积模拟图表。
- 机构表作为核心工作区，占据主要宽度。
- 配置编辑只在保存后生效，并显示保存时间/失败状态。
- “每 30 秒更新”只在真实刷新服务存在时展示。
- 预留和可用趋势没有真实历史时显示未连接状态，不生成曲线。

### 13.5 新增 Web 交互

- 编辑金额/折扣时在同一卡片内进入编辑状态，取消恢复原值。
- 搜索、状态和排序保存在页面 URL。
- 点击机构行展开该行完整配额信息，但不创建新的预留业务操作。
- 当前窗口状态由 CDMX 时间统一计算并显示下一时间节点。

### 13.6 页面结构建议

```text
顶部：专用品牌 + CDMX 时间 + 当前窗口
第一层：Monto Total / Descuento / Reservadas / Disponible / BMV
主体：机构状态表
右侧：Ventanas Institucionales + Calendario Operativo
底部：有真实数据时显示容量趋势
```

### 13.7 删除或隐藏

- 删除硬编码机构、金额、预留股数和趋势。
- 隐藏假的“实时更新”和固定倒计时文本。
- 不提供普通人物、Compra、Venta 或持仓联动。

## 14. Centro de Plantillas 升级方案

### 14.1 原桌面功能

- 四类内置模板：机构单栏、机构双栏、买入、卖出。
- 模板统计、分类筛选、搜索、上传、新建、使用和删除。
- 模板编辑器：文字、图片、Logo、背景、图形、图层、属性、撤销/重做、缩放、保存、4K/8K PNG。
- 读取最新订单、卖出和 VIP 数据作为预览。

### 14.2 Web V2 保留

- 全部现有模板分类与模板能力。
- 上传格式、拖放、目录、编辑、保存、删除和 PNG 导出。
- 买入、卖出和 VIP 的真实数据预览。
- Pi Logo 与机构 Logo 的使用边界。

### 14.3 Web V2 优化

- 模板目录先显示快速预览和基本信息，避免每次都进入编辑器。
- 搜索和分类放在单一工具栏，统计卡压缩为一行。
- 编辑器保持资产栏、画布、属性栏三栏结构，但减少重复控制。
- 保存状态必须区分未保存、保存中、已保存和失败。
- 工程和预览引用真实业务数据，但保存模板时不复制业务记录。

### 14.4 新增 Web 交互

- 点击模板先打开快速预览；“使用模板”才进入编辑。
- 上传前显示格式和尺寸校验结果。
- 离开未保存工程时提醒；已保存工程可恢复上次编辑位置。
- 模板数据预览允许选择现有买入订单或卖出记录，不生成示例交易。
- 导出前显示尺寸、文件名和 Logo 类型确认。

### 14.5 页面结构建议

```text
/templates 目录
├── 统计与搜索/分类
├── 模板列表或高密度卡片
└── 快速预览抽屉

编辑工作区
├── 左：素材与模板
├── 中：16:9 画布
├── 右：对象/图层/页面属性
└── 顶：撤销、重做、保存、预览、导出
```

### 14.6 删除或隐藏

- 不显示假的“自动保存已开启”。
- 无真实订单/卖出记录时显示“暂无数据”，不填入模拟股票和价格。
- 不新增社交发布、审批或模板商城。

## 15. Reportes 补充方案

### 15.1 角色定位

Reportes 是 Dashboard 指标的解释和明细层，不是另一个重复 Dashboard。它回答指标如何变化、由哪些人物/交易构成，并提供原系统已有导出能力。

### 15.2 原桌面功能

- 总人物、活跃人物、交易记录、持仓价值、剧本记录。
- 交易参与、人物活跃、资金和收益趋势。
- 业务构成和收益分布。
- 最近交易表、买入/卖出/盈利/亏损筛选和搜索。
- CSV、JSON、Excel 外观文件和打印/PDF。

### 15.3 Web V2 保留

- 原统计范围、7/30/90/年度时间范围、趋势、明细和导出。
- 人物、交易、持仓、剧本四类数据关系。
- 买入、卖出、盈利、亏损和搜索筛选。

### 15.4 Web V2 优化

- 新增明确 `/reports` 页面，Sidebar 使用 Reportes/数据分析入口。
- Dashboard 与 Reportes 共用统一指标定义和 CDMX 日期边界。
- 报表区分运营、资产和交易三个区块，避免不同单位混在同一图中。
- 美股金额按交易时或已确认估值汇率换算 MXN，并保留原币说明。
- 大数据范围由聚合服务返回，不在浏览器扫描全部记录。
- 导出格式统一由一个导出入口管理，避免重复触发多个文件。

### 15.5 新增 Web 交互

- 从 Dashboard 指标下钻时自动选中对应 Reportes 区块和时间范围。
- 图表点击可过滤下面的交易/人物明细，但不改变原始数据。
- 当前筛选显示为可清除条件条，导出只包含当前条件或明确选择全部。
- 空数据区显示产生该类数据的现有入口，例如进入 Compra，而不是模拟趋势。

### 15.6 页面结构建议

```text
顶部：时间范围 + 导出
第一层：运营 KPI（人物、活跃、交易、剧本）
第二层：运营趋势 + 资产趋势
第三层：业务构成 + 收益分布
第四层：最近交易明细与筛选
```

### 15.7 删除或隐藏

- 删除 `amount × 4%` 的模拟盈利计算。
- 删除固定阈值生成的业务构成百分比。
- 删除不考虑 USD/MXN 的跨市场金额汇总。
- 不新增预测、收益承诺或未经确认的绩效指标。

## 16. 跨模块组件规划

以下是交互组件，不是新增业务模块：

| 组件 | 用途 | 使用页面 |
|---|---|---|
| OperationalKpi | 统一指标、状态、更新时间和下钻 | Dashboard、Reportes |
| PersonTradeCard | 人物身份、规则、资金/持仓与选择状态 | Compra、Venta |
| TradeSummaryRail | 交易标的、价格、人数、金额、股数、确认 | Compra、Venta |
| PositionTable | 股票聚合与人物持仓展开 | Posiciones、人物详情 |
| MarketStatus | 指数、涨跌、市场状态、更新时间 | Dashboard、两个市场页 |
| DataState | 加载、空、断开、失败、重试 | 所有数据页面 |
| FilterToolbar | 搜索、筛选、重置和当前条件 | Clients、Posiciones、Reportes、Institutional |
| ExportAction | 当前条件/全部数据导出 | Posiciones、Venta、Reportes、Templates |

## 17. 关键产品规则

1. 页面可以合并，但数据校验步骤不能删除。
2. 交易摘要必须来自当前页面输入和真实数据，不使用截图数字。
3. 页面刷新可恢复草稿 UI 状态，但不能把草稿当成已执行交易。
4. 人物卡在 Compra 与 Venta 共享视觉结构，不共享资格逻辑。
5. 市场指数、个股报价和 USD/MXN 是三类不同数据。
6. Reserva Institucional 不进入普通人物、持仓和交易数据域。
7. 模板可以读取交易摘要，但不能修改订单或人物资金。
8. Dashboard 和 Reportes 只能使用同一套指标定义。
9. 所有金额显示货币；跨市场汇总统一使用 MXN。
10. 所有“实时”状态必须有真实来源和更新时间。

## 18. 需要确认的产品决策

在开发 Compra、Venta 和 Reportes 前，需要确认以下业务口径：

1. Compra 严格资格池为空时，是否保留桌面端“退回全体人物”的兜底。
2. 买入日期是有效业务字段，还是应从界面移除。
3. Venta 的 0.2% 手续费是否真实扣除人物余额。
4. Venta 的 18:00–20:00、T+2 和协议是否需要进入交易记录。
5. Dashboard 第四张资产卡使用“今日盈亏”还是“浮动盈亏”，以及真实计算定义。
6. Reportes 采用新增 `/reports` 独立路由是否确认。
7. 模板工程是否需要真实自动保存，或继续手动保存。
8. Reserva Institucional 的真实机构数据来源和刷新频率。

## 19. 产品验收标准

- 用户从 Dashboard 最多一次点击进入目标业务上下文。
- 人物目录返回后保留筛选、分页和滚动位置。
- Compra 通过“配置与推荐同屏 + 一次最终确认”完成，但保留所有资格校验。
- Venta 从持仓进入后无需重复选择股票。
- 持仓、人物、交易和报表可以互相追溯。
- 两个市场页面结构一致，货币、时区和指数正确区分。
- 无真实数据时没有模拟客户、金额、趋势、机构或行情。
- Reserva Institucional 与普通交易数据完全隔离。
- 模板只读取真实业务摘要，不改写业务数据。
- Dashboard 与 Reportes 同一指标得到相同结果。

## 20. 建议确认顺序

1. 确认新版一级导航和 `/reports`。
2. 确认 Dashboard 四层结构。
3. 确认人物中心字段保留与“今日发言”口径。
4. 确认 Compra 两阶段 Web 工作台和人员卡结构。
5. 确认 Venta 手续费、窗口和结算字段。
6. 确认持仓作为 Centro de Operaciones 子视图。
7. 确认市场、模板和机构预留的后续优先级。

